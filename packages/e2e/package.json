{
  "private": true,
  "name": "e2e",
  "version": "1.0.0",
  "scripts": {
    "test": "dotenv -e .env jest --runInBand"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "jest-puppeteer": "^4.3.0",
    "puppeteer": "^1.19.0"
  },
  "jest": {
    "verbose": true,
    "preset": "jest-puppeteer",
    "setupFilesAfterEnv": []
  },
  "dependencies": {
    "dotenv-cli": "^2.0.1"
  }
}